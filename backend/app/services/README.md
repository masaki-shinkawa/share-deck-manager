# Service Layer

This directory contains the business logic of the application.

Services should handle complex operations, independent of the HTTP transport layer. They can be injected into API endpoints.
