# API Layer

This directory contains the API route definitions.

- `v1/`: Version 1 of the API.
    - `api.py`: Central router aggregator for V1.
    - `endpoints/`: Individual route modules (e.g., `users.py`, `items.py`).
