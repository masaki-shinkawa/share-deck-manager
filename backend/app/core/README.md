# Core Configuration

This directory handles the core settings and configuration of the application.

- `config.py`: Application settings (loading env vars).
- `security.py`: Authentication and security utilities (JWT, password hashing).
- `logging.py`: Logging configuration.
