# Pydantic Schemas

This directory contains Pydantic models used for:
- Request validation (API input).
- Response serialization (API output).

Separate schemas are often used for Creating, Updating, and Reading resources (e.g., `ItemCreate`, `ItemUpdate`, `ItemRead`).
